# glab-flow 初始设计（历史索引）

这是早期设计快照，已被当前状态机、纯计算 engine 与 `skills/glab-flow/` 的执行契约取代。GitLab Issue 的标签与评论仍是运行时真理；Leader 负责 glab I/O，引擎只做确定性计算。

请查阅 `docs/architecture.md`、`docs/flow.md` 和当前 skill 文件，不要把本历史索引当作可执行设计。
