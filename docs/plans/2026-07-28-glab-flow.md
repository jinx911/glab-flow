# glab-flow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `glab-flow`, a local human-driven Claude skill that drives an OA requirement through the `oa-ai-native-harness` GitLab Issue state machine (triage → done) with deterministic guardrails, content generation via reused sub-skills, and preview-confirmed GitLab writeback.

**Architecture:** Hybrid. A small tested **TypeScript engine** (`engine/`) holds the deterministic core — state-machine model, guard validator (G1–G13), GitLab client, comment renderer. A **markdown skill/agent pack** (`skills/glab-flow/SKILL.md` + `agents/*.md` + `install.sh`) orchestrates: the Leader reads GitLab, calls the engine CLI for state/guard, delegates expert agents (reusing spec-author/git-ops/code-review/tdd-guide), previews the write plan, confirms, applies. GitLab Issue is the only truth source.

**Tech Stack:** TypeScript (strict, NodeNext), vitest, pnpm, `yaml` + `zod` (model & payload validation), Node global fetch, tsx (dev CLI). Skill pack = markdown, installed by symlink (matches jira-flow/quick-dev-flow pattern).

**Spec:** `/Users/eliojin/IdeaProjects/oa/docs/superpowers/specs/2026-07-28-glab-flow-design.md`

---

## File Structure

```
glab-flow/                                  # /Users/eliojin/IdeaProjects/glab-flow (new git repo)
  package.json                              # pnpm, scripts: test/cli/typecheck
  tsconfig.json                             # strict, NodeNext
  vitest.config.ts
  .gitignore
  README.md
  install.sh / uninstall.sh                 # symlink skills+agents into ~/.claude
  engine/
    state-machine.yaml                      # the model (spec §5), derived from harness
    src/
      types.ts                              # Transition, Payload, IssueFacts, GuardResult, WriteOp
      model.ts                              # load yaml + query: currentNode/transitionFor/requiredFields/assigneeRole/returnTarget
      model.test.ts
      contract.ts                           # assertModelContract: yaml <-> invariants (drift check)
      contract.test.ts
      guard.ts                              # validateTransition (G1,G2,G3,G4,G5,G6,G9,G10,G11,G12) + validateWritePlan (G7,G8,G13)
      guard.test.ts
      gitlab.ts                             # parse GitLab issue facts / 交付协同 (no network/writeback)
      gitlab.test.ts
      render.ts                             # structured payload -> harness markdown templates
      render.test.ts
      cli.ts                                # pure subcommands: node / validate / render / plan / plan-return / evidence / config / state-init
    fixtures/
      issue-story-draft.json
      issue-story-pending-review.json
      writeplan-approve.json
  skills/
    glab-flow/
      SKILL.md                              # Leader orchestration (spec §6.1 algorithm)
      nodes.md                              # per-node agent+gate+writeback reference (spec §6.2)
      guards.md                             # G1–G13 plain-language reference for the Leader
  agents/
    intake.md                               # triage/clarify (harness oa-knowledge-intake)
    review-preview.md                       # 需求评审预审 + 问题清单
    release-check.md                        # 发布检查 (harness oa-release-check)
  docs/
    architecture.md                         # copy of spec §4 diagram + dataflow
```

**Responsibility boundaries:** `model.ts` answers "what state am I in / what's allowed" (read-only query). `guard.ts` answers "may I apply this payload/plan?" (pure predicate, no I/O). `gitlab.ts` is the only I/O layer. `render.ts` is pure string building. `cli.ts` wires them. Skill markdown never re-implements rules — it calls the CLI.

---

## Phase 0 — Repo init + tooling

### Task 0.1: Initialize glab-flow repo and TS toolchain

**Files:**
- Create: `glab-flow/package.json`, `glab-flow/tsconfig.json`, `glab-flow/vitest.config.ts`, `glab-flow/.gitignore`, `glab-flow/README.md`

- [ ] **Step 1: Create the repo dir and verify toolchain**

Run:
```bash
mkdir -p /Users/eliojin/IdeaProjects/glab-flow
cd /Users/eliojin/IdeaProjects/glab-flow
node -v   # expect v24.x
pnpm -v   # expect 10.x
git init -b master
```

- [ ] **Step 2: Write `package.json`**

```json
{
  "name": "glab-flow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OA GitLab Issue state-machine driven delivery skill (harness executor)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "cli": "tsx engine/src/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
```

- [ ] **Step 3: Write `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "outDir": "engine/dist",
    "rootDir": "engine/src"
  },
  "include": ["engine/src/**/*.ts"]
}
```

- [ ] **Step 4: Write `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({
  test: { include: ['engine/**/*.test.ts'], environment: 'node' },
});
```

- [ ] **Step 5: Write `.gitignore`**

```
node_modules/
engine/dist/
.glab-flow/
.DS_Store
*.log
```

- [ ] **Step 6: Write `README.md` (skeleton)**

```markdown
# glab-flow

Local human-driven Claude skill that drives OA requirements through the
`oa-ai-native-harness` GitLab Issue state machine with deterministic guardrails.

- Design: see repo docs / spec.
- Engine (tested TS): `engine/`
- Skill pack: `skills/glab-flow/`, installed via `install.sh`

## Install
\`\`\`bash
./install.sh
\`\`\`
```

- [ ] **Step 7: Install deps and verify**

Run:
```bash
cd /Users/eliojin/IdeaProjects/glab-flow
pnpm install
pnpm test 2>&1 | head -5   # expect "No test files found" (vitest) — OK for now
```
Expected: install succeeds; vitest reports no tests yet.

- [ ] **Step 8: Commit**

```bash
cd /Users/eliojin/IdeaProjects/glab-flow
git add -A
git commit -m "chore: init glab-flow repo + TS/vitest toolchain"
```

---

## Phase 1 — State machine model + consistency contract (TDD)

### Task 1.1: Write the state machine model YAML

**Files:**
- Create: `engine/state-machine.yaml`

- [ ] **Step 1: Write `engine/state-machine.yaml`** (verbatim from spec §5)

```yaml
# glab-flow state machine — executable projection of oa-ai-native-harness
# docs/issue-state-machine.md. Drift checked by engine/src/contract.ts.
story:
  states: [草稿中, 待评审, 已评审, 开发中, 测试中, 待发布, 生产验收中, 已完成]
  transitions:
    - {from: 草稿中, to: 待评审, gate: null, requiredFields: [], assigneeRole: 产品}
    - {from: 待评审, to: 已评审, gate: 需求评审, gateOutcome: [通过, 退回],
       requiredFields: [评审日期, 产品确认人, 评审结论, 需求文档或评审记录],
       assigneeRole: 研发,
       return: {target: 草稿中, assigneeRole: 产品}}
    - {from: 已评审, to: 开发中, gate: 技术方案评审,
       requiredFields: [技术方案评审通过记录或免评审结论, 实际开始日期, 研发Assignee, 计划提测时间, 计划上线时间],
       assigneeRole: 研发,
       return: {target: 已评审, note: 不退回, 继续完善方案}}
    - {from: 开发中, to: 测试中, gate: 代码评审与自测,
       requiredFields: [代码评审与自测结论, 提测日期, 研发Assignee, 可测试版本或环境, 测试说明],
       assigneeRole: 测试}
    - {from: 测试中, to: 待发布, gate: 测试验收,
       requiredFields: [测试完成日期, 测试Assignee, 测试结论, 回归范围或证据, 阻塞发布问题均已验证通过],
       assigneeRole: 研发,
       return: {target: 开发中, onlyWhen: 整体返工}}
    - {from: 待发布, to: 生产验收中, gate: 发布, hardGate: true,
       requiredFields: [发布日期, 研发Assignee, 生产版本, 发布记录或回滚信息],
       assigneeRole: 产品}
    - {from: 生产验收中, to: 已完成, gate: 产品验收, hardGate: true, terminal: true,
       requiredFields: [验收完成日期, 具体产品验收人, 产品Assignee, 验收结论, 验收依据],
       assigneeRole: 产品}
bug:
  states: [已确认缺陷, 开发中, 测试中, 待发布, 生产验证中, 已完成]
  transitions:
    - {from: 已确认缺陷, to: 开发中, gate: null, requiredFields: [], assigneeRole: 研发}
    - {from: 开发中, to: 测试中, gate: 代码评审与自测,
       requiredFields: [代码评审与自测结论, 提测日期, 研发Assignee, 可测试版本或环境],
       assigneeRole: 测试}
    - {from: 测试中, to: 待发布, gate: 测试验收,
       requiredFields: [测试完成日期, 测试Assignee, 测试结论, 回归范围或证据],
       assigneeRole: 研发}
    - {from: 待发布, to: 生产验证中, gate: 发布, hardGate: true,
       requiredFields: [发布日期, 研发Assignee, 生产版本, 发布记录或回滚信息],
       assigneeRole: 测试}
    - {from: 生产验证中, to: 已完成, gate: 生产验证, hardGate: true, terminal: true,
       requiredFields: [验证完成日期, 具体测试验证人, 测试验证人Assignee, 验证结论, 验证依据],
       assigneeRole: 测试}
reviews:
  需求评审: 推进状态
  技术方案评审: 只记录不推进
  代码评审: 不单独推进
roleFields:
  产品: [产品确认人, 具体产品验收人, 产品Assignee]
  研发: [研发Assignee]
  测试: [测试Assignee, 具体测试验证人, 测试验证人Assignee]
```

> Note: YAML keys are simplified to valid identifiers (no `::`, no CJK punctuation in keys). Values keep CJK.

- [ ] **Step 2: Commit**

```bash
git add engine/state-machine.yaml
git commit -m "feat(model): add state-machine.yaml (story+bug, derived from harness)"
```

### Task 1.2: Types + model loader/query (TDD)

**Files:**
- Create: `engine/src/types.ts`, `engine/src/model.ts`, `engine/src/model.test.ts`

- [ ] **Step 1: Write the failing test `engine/src/model.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { loadModel, currentNode, transitionFor, requiredFields, assigneeRole, returnTarget } from './model.js';

const model = loadModel();

describe('model', () => {
  it('derives current story node from labels', () => {
    expect(currentNode(model, 'story', ['type::story', 'story-status::待评审'])).toBe('待评审');
  });
  it('returns null when no story-status label', () => {
    expect(currentNode(model, 'story', ['type::story'])).toBeNull();
  });
  it('finds a forward transition', () => {
    const t = transitionFor(model, 'story', '待评审', '已评审');
    expect(t?.gate).toBe('需求评审');
    expect(t?.gateOutcome).toEqual(['通过', '退回']);
  });
  it('lists required fields for a transition', () => {
    expect(requiredFields(model, 'story', '待评审', '已评审')).toContain('评审日期');
  });
  it('returns target-node assignee role', () => {
    expect(assigneeRole(model, 'story', '待评审', '已评审')).toBe('研发');
  });
  it('returns return target for a gated transition', () => {
    expect(returnTarget(model, 'story', '待评审', '已评审')?.target).toBe('草稿中');
  });
  it('marks hard-gate transitions', () => {
    expect(transitionFor(model, 'story', '待发布', '生产验收中')?.hardGate).toBe(true);
  });
  it('marks terminal transitions', () => {
    expect(transitionFor(model, 'story', '生产验收中', '已完成')?.terminal).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run engine/src/model.test.ts`
Expected: FAIL — "cannot find module ./model.js" / functions undefined.

- [ ] **Step 3: Write `engine/src/types.ts`**

```ts
export type IssueType = 'story' | 'bug';
export type Role = '产品' | '研发' | '测试';

export interface Transition {
  from: string;
  to: string;
  gate: string | null;
  gateOutcome?: string[];
  requiredFields: string[];
  assigneeRole: Role;
  hardGate?: boolean;
  terminal?: boolean;
  return?: { target: string; assigneeRole: Role; onlyWhen?: string; note?: string };
}

export interface StateMachine {
  story: { states: string[]; transitions: Transition[] };
  bug: { states: string[]; transitions: Transition[] };
  reviews: Record<string, string>;
  roleFields: Record<Role, string[]>;
}

export interface Payload {
  type: IssueType;
  from: string;
  to: string;
  fields: Record<string, string>;
  gateOutcome?: '通过' | '退回';
  reviewType?: string;          // 需求评审 | 技术方案评审 | 代码评审
  assigneeUser?: string;        // concrete @user resolved from 交付协同
  datesConfirmed?: boolean;
  humanConfirmed?: boolean;     // for hardGate
  closeIssue?: boolean;         // for terminal
}

export interface IssueFacts {
  labels: string[];
  body: string;
  state: 'opened' | 'closed';
  hasJiraSourceLabel: boolean;
}

export type WriteOp =
  | { kind: 'add_label'; value: string }
  | { kind: 'remove_label'; value: string }
  | { kind: 'set_assignee'; username: string }
  | { kind: 'add_comment'; body: string }
  | { kind: 'close_issue' };

export interface WritePlan {
  issueIid: number;
  ops: WriteOp[];
}

export interface GuardResult {
  ok: boolean;
  missing: string[];
  reasons: string[];
}
```

- [ ] **Step 4: Write `engine/src/model.ts`**

```ts
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import { parse } from 'yaml';
import type { IssueType, StateMachine, Transition } from './types.js';

const __dirname = dirname(fileURLToPath(import.meta.url));
const MODEL_PATH = join(__dirname, '..', 'state-machine.yaml');

export function loadModel(path: string = MODEL_PATH): StateMachine {
  const raw = readFileSync(path, 'utf8');
  return parse(raw) as StateMachine;
}

const STATUS_PREFIX: Record<IssueType, string> = { story: 'story-status::', bug: 'status::' };

export function currentNode(model: StateMachine, type: IssueType, labels: string[]): string | null {
  const prefix = STATUS_PREFIX[type];
  const found = labels.find((l) => l.startsWith(prefix));
  return found ? found.slice(prefix.length) : null;
}

function transitions(model: StateMachine, type: IssueType): Transition[] {
  return model[type].transitions;
}

export function transitionFor(model: StateMachine, type: IssueType, from: string, to: string): Transition | undefined {
  return transitions(model, type).find((t) => t.from === from && t.to === to);
}

export function requiredFields(model: StateMachine, type: IssueType, from: string, to: string): string[] {
  return transitionFor(model, type, from, to)?.requiredFields ?? [];
}

export function assigneeRole(model: StateMachine, type: IssueType, from: string, to: string) {
  return transitionFor(model, type, from, to)?.assigneeRole;
}

export function returnTarget(model: StateMachine, type: IssueType, from: string, to: string) {
  return transitionFor(model, type, from, to)?.return;
}

export function allowedTransitions(model: StateMachine, type: IssueType, from: string): Transition[] {
  return transitions(model, type).filter((t) => t.from === from);
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `pnpm vitest run engine/src/model.test.ts`
Expected: PASS (8 tests).

- [ ] **Step 6: Commit**

```bash
git add engine/src/types.ts engine/src/model.ts engine/src/model.test.ts
git commit -m "feat(model): load state-machine.yaml + node/transition queries"
```

### Task 1.3: Model ↔ invariants consistency contract (TDD)

**Files:**
- Create: `engine/src/contract.ts`, `engine/src/contract.test.ts`

> Purpose: catch drift between `state-machine.yaml` and the harness rules. A checked-in invariant list is the human-readable contract; the test asserts the yaml satisfies it. When harness rules change, update yaml + invariants together.

- [ ] **Step 1: Write the failing test `engine/src/contract.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { loadModel } from './model.js';
import { assertModelContract, INVARIANTS } from './contract.js';

describe('contract', () => {
  it('state-machine.yaml satisfies the checked-in invariants', () => {
    const result = assertModelContract(loadModel(), INVARIANTS);
    expect(result.ok, result.reasons.join('\n')).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run engine/src/contract.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Write `engine/src/contract.ts`**

```ts
import type { StateMachine, IssueType } from './types.js';
import { transitionFor } from './model.js';

export interface Invariants {
  storyStates: string[];
  bugStates: string[];
  // [type, from, to] transitions that MUST exist
  transitions: [IssueType, string, string][];
  hardGates: [IssueType, string, string][];
  terminals: [IssueType, string, string][];
  returns: { type: IssueType; from: string; to: string; target: string }[];
}

export const INVARIANTS: Invariants = {
  storyStates: ['草稿中', '待评审', '已评审', '开发中', '测试中', '待发布', '生产验收中', '已完成'],
  bugStates: ['已确认缺陷', '开发中', '测试中', '待发布', '生产验证中', '已完成'],
  transitions: [
    ['story', '草稿中', '待评审'], ['story', '待评审', '已评审'], ['story', '已评审', '开发中'],
    ['story', '开发中', '测试中'], ['story', '测试中', '待发布'], ['story', '待发布', '生产验收中'],
    ['story', '生产验收中', '已完成'],
    ['bug', '已确认缺陷', '开发中'], ['bug', '开发中', '测试中'], ['bug', '测试中', '待发布'],
    ['bug', '待发布', '生产验证中'], ['bug', '生产验证中', '已完成'],
  ],
  hardGates: [['story', '待发布', '生产验收中'], ['story', '生产验收中', '已完成'],
              ['bug', '待发布', '生产验证中'], ['bug', '生产验证中', '已完成']],
  terminals: [['story', '生产验收中', '已完成'], ['bug', '生产验证中', '已完成']],
  returns: [
    { type: 'story', from: '待评审', to: '已评审', target: '草稿中' },
    { type: 'story', from: '测试中', to: '待发布', target: '开发中' },
  ],
};

export function assertModelContract(model: StateMachine, inv: Invariants): { ok: boolean; reasons: string[] } {
  const reasons: string[] = [];
  const eq = (a: string[], b: string[]) => a.length === b.length && a.every((x, i) => x === b[i]);
  if (!eq(model.story.states, inv.storyStates)) reasons.push('story states drift');
  if (!eq(model.bug.states, inv.bugStates)) reasons.push('bug states drift');
  for (const [t, f, to] of inv.transitions) if (!transitionFor(model, t, f, to)) reasons.push(`missing transition ${t} ${f}->${to}`);
  for (const [t, f, to] of inv.hardGates) if (!transitionFor(model, t, f, to)?.hardGate) reasons.push(`expected hardGate ${t} ${f}->${to}`);
  for (const [t, f, to] of inv.terminals) if (!transitionFor(model, t, f, to)?.terminal) reasons.push(`expected terminal ${t} ${f}->${to}`);
  for (const r of inv.returns) {
    const rt = transitionFor(model, r.type, r.from, r.to)?.return;
    if (!rt || rt.target !== r.target) reasons.push(`return target drift ${r.type} ${r.from}->${r.to} (want ${r.target})`);
  }
  return { ok: reasons.length === 0, reasons };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run engine/src/contract.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add engine/src/contract.ts engine/src/contract.test.ts
git commit -m "feat(model): add contract drift check (yaml <-> invariants)"
```

---

## Phase 2 — Guard layer G1–G13 (TDD, pure functions)

### Task 2.1: Transition guards G1, G2, G3, G4 (TDD)

**Files:**
- Create: `engine/src/guard.ts`, `engine/src/guard.test.ts`

- [ ] **Step 1: Write failing tests (append to `guard.test.ts`)**

```ts
import { describe, it, expect } from 'vitest';
import { loadModel } from './model.js';
import { validateTransition } from './guard.js';
import type { IssueFacts, Payload } from './types.js';

const model = loadModel();
const facts = (labels: string[]): IssueFacts => ({ labels, body: '', state: 'opened', hasJiraSourceLabel: false });

describe('G1 required fields', () => {
  it('blocks when a required field is missing', () => {
    const p: Payload = { type: 'story', from: '待评审', to: '已评审', fields: { 评审日期: '2026-07-28' }, gateOutcome: '通过', assigneeUser: '@dev', datesConfirmed: true };
    const r = validateTransition(model, facts(['type::story', 'story-status::待评审']), p);
    expect(r.ok).toBe(false);
    expect(r.missing).toContain('产品确认人');
  });
  it('passes when all required fields present', () => {
    const p: Payload = { type: 'story', from: '待评审', to: '已评审',
      fields: { 评审日期: '2026-07-28', 产品确认人: '@pm', 评审结论: '通过', 需求文档或评审记录: 'link' },
      gateOutcome: '通过', reviewType: '需求评审', assigneeUser: '@dev', datesConfirmed: true };
    const r = validateTransition(model, facts(['type::story', 'story-status::待评审']), p);
    expect(r.ok).toBe(true);
  });
});

describe('G2 gate outcome binary', () => {
  it('blocks forward transition when gateOutcome is 退回', () => {
    const p: Payload = { type: 'story', from: '待评审', to: '已评审', fields: {}, gateOutcome: '退回' };
    const r = validateTransition(model, facts(['type::story', 'story-status::待评审']), p);
    expect(r.ok).toBe(false);
    expect(r.reasons.some((x) => x.includes('退回'))).toBe(true);
  });
});

describe('G3 hard gate needs humanConfirmed', () => {
  it('blocks hard-gate transition without humanConfirmed', () => {
    const p: Payload = { type: 'story', from: '待发布', to: '生产验收中',
      fields: { 发布日期: '2026-07-28', 研发Assignee: '@dev', 生产版本: 'v1', 发布记录或回滚信息: 'rec' },
      assigneeUser: '@pm', datesConfirmed: true };
    const r = validateTransition(model, facts(['type::story', 'story-status::待发布']), p);
    expect(r.ok).toBe(false);
    expect(r.reasons.some((x) => x.includes('hard'))).toBe(true);
  });
});

describe('G4 three-review separation', () => {
  it('blocks 需求评审 transition when reviewType is 技术方案评审', () => {
    const p: Payload = { type: 'story', from: '待评审', to: '已评审',
      fields: { 评审日期: '2026-07-28', 产品确认人: '@pm', 评审结论: '通过', 需求文档或评审记录: 'link' },
      gateOutcome: '通过', reviewType: '技术方案评审', assigneeUser: '@dev', datesConfirmed: true };
    const r = validateTransition(model, facts(['type::story', 'story-status::待评审']), p);
    expect(r.ok).toBe(false);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run engine/src/guard.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Write `engine/src/guard.ts` (transition guards first)**

```ts
import type { StateMachine, IssueFacts, Payload, GuardResult } from './types.js';
import { transitionFor } from './model.js';

const ok: GuardResult = { ok: true, missing: [], reasons: [] };
const fail = (reasons: string[], missing: string[] = []): GuardResult => ({ ok: false, missing, reasons });

export function validateTransition(model: StateMachine, facts: IssueFacts, payload: Payload): GuardResult {
  const t = transitionFor(model, payload.type, payload.from, payload.to);
  if (!t) return fail([`transition ${payload.from}->${payload.to} not allowed`]);

  const missing: string[] = [];
  const reasons: string[] = [];

  // G1 required fields (non-empty, not placeholder)
  for (const f of t.requiredFields) {
    const v = payload.fields[f];
    if (v === undefined || v === '' || v === '待确认') missing.push(f);
  }

  // G2 gate outcome binary (forward requires 通过)
  if (t.gateOutcome && payload.gateOutcome !== '通过') {
    return fail([`gate ${t.gate} 结论为 ${payload.gateOutcome ?? '未定'}，需走退回路径`], missing);
  }

  // G3 hard gate needs explicit human confirmation
  if (t.hardGate && !payload.humanConfirmed) {
    reasons.push(`hard-gate ${t.gate} 需人工确认证据(humanConfirmed)`);
  }

  // G4 review type must match the gate (prevents 技评/代码评审 substituting for 需求评审)
  if (t.gate && payload.reviewType && t.gate !== payload.reviewType) {
    reasons.push(`reviewType ${payload.reviewType} 与门禁 ${t.gate} 不符`);
  }

  if (missing.length || reasons.length) return fail(reasons, missing);
  return ok;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm vitest run engine/src/guard.test.ts`
Expected: PASS (the tests written so far).

- [ ] **Step 5: Commit**

```bash
git add engine/src/guard.ts engine/src/guard.test.ts
git commit -m "feat(guard): G1 required fields, G2 binary gate, G3 hard gate, G4 review separation"
```

### Task 2.2: Facts guards G5, G6, G9, G10, G11, G12 (TDD)

**Files:**
- Modify: `engine/src/guard.ts`, `engine/src/guard.test.ts`

- [ ] **Step 1: Append failing tests**

```ts
describe('G5 label uniqueness', () => {
  it('blocks when two story-status labels present (dirty state)', () => {
    const p: Payload = { type: 'story', from: '草稿中', to: '待评审', fields: {} };
    const r = validateTransition(model, facts(['type::story', 'story-status::草稿中', 'story-status::待评审']), p);
    expect(r.ok).toBe(false);
    expect(r.reasons.some((x) => x.includes('脏状态'))).toBe(true);
  });
});

describe('G6 assignee concrete user', () => {
  it('blocks when assigneeUser is a bare role, not a user', () => {
    const p: Payload = { type: 'story', from: '草稿中', to: '待评审', fields: {}, assigneeUser: '研发' };
    const r = validateTransition(model, facts(['type::story', 'story-status::草稿中']), p);
    expect(r.ok).toBe(false);
    expect(r.reasons.some((x) => x.includes('Assignee'))).toBe(true);
  });
});

describe('G9 no placeholder people/conclusion', () => {
  it('blocks when a person field is 待确认', () => {
    const p: Payload = { type: 'story', from: '待评审', to: '已评审',
      fields: { 评审日期: '2026-07-28', 产品确认人: '待确认', 评审结论: '通过', 需求文档或评审记录: 'link' },
      gateOutcome: '通过', reviewType: '需求评审', assigneeUser: '@dev', datesConfirmed: true };
    const r = validateTransition(model, facts(['type::story', 'story-status::待评审']), p);
    expect(r.ok).toBe(false);
    expect(r.missing).toContain('产品确认人');
  });
});

describe('G10 date confirmation', () => {
  it('blocks when a date field present but datesConfirmed false', () => {
    const p: Payload = { type: 'story', from: '待评审', to: '已评审',
      fields: { 评审日期: '2026-07-28', 产品确认人: '@pm', 评审结论: '通过', 需求文档或评审记录: 'link' },
      gateOutcome: '通过', reviewType: '需求评审', assigneeUser: '@dev', datesConfirmed: false };
    const r = validateTransition(model, facts(['type::story', 'story-status::待评审']), p);
    expect(r.ok).toBe(false);
  });
});

describe('G11 blocking test issues verified', () => {
  it('blocks 测试中->待发布 when blocking issues not verified', () => {
    const p: Payload = { type: 'story', from: '测试中', to: '待发布',
      fields: { 测试完成日期: '2026-07-28', 测试Assignee: '@qa', 测试结论: '通过', 回归范围或证据: 'r', 阻塞发布问题均已验证通过: '否' },
      assigneeUser: '@dev', datesConfirmed: true };
    const r = validateTransition(model, facts(['type::story', 'story-status::测试中']), p);
    expect(r.ok).toBe(false);
  });
});

describe('G12 terminal atomicity', () => {
  it('blocks terminal transition without closeIssue', () => {
    const p: Payload = { type: 'story', from: '生产验收中', to: '已完成',
      fields: { 验收完成日期: '2026-07-28', 具体产品验收人: '@pm', 产品Assignee: '@pm', 验收结论: '通过', 验收依据: 'ok' },
      assigneeUser: '@pm', datesConfirmed: true, humanConfirmed: true, closeIssue: false };
    const r = validateTransition(model, facts(['type::story', 'story-status::生产验收中']), p);
    expect(r.ok).toBe(false);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run engine/src/guard.test.ts`
Expected: FAIL (new tests).

- [ ] **Step 3: Extend `validateTransition` in `engine/src/guard.ts`**

Replace the function body's tail (before `if (missing.length...)`) and add checks. Final `validateTransition`:

```ts
export function validateTransition(model: StateMachine, facts: IssueFacts, payload: Payload): GuardResult {
  const t = transitionFor(model, payload.type, payload.from, payload.to);
  if (!t) return fail([`transition ${payload.from}->${payload.to} not allowed`]);

  const missing: string[] = [];
  const reasons: string[] = [];

  // G5 label uniqueness + clean state
  const prefix = payload.type === 'story' ? 'story-status::' : 'status::';
  const statusLabels = facts.labels.filter((l) => l.startsWith(prefix));
  if (statusLabels.length !== 1) return fail([`脏状态：期望 1 个 ${prefix}* 标签，实际 ${statusLabels.length} 个（人工修复后继续）`]);
  if (facts.labels.filter((l) => l.startsWith('type::')).length !== 1) return fail(['脏状态：期望 1 个 type::* 标签']);

  // G1 required fields (G9: no placeholder 待确认)
  for (const f of t.requiredFields) {
    const v = payload.fields[f];
    if (v === undefined || v === '' || v === '待确认') missing.push(f);
  }

  // G2 gate outcome binary
  if (t.gateOutcome && payload.gateOutcome !== '通过') {
    return fail([`gate ${t.gate} 结论为 ${payload.gateOutcome ?? '未定'}，需走退回路径`], missing);
  }

  // G3 hard gate
  if (t.hardGate && !payload.humanConfirmed) reasons.push(`hard-gate ${t.gate} 需人工确认证据(humanConfirmed)`);

  // G4 review type matches gate
  if (t.gate && payload.reviewType && t.gate !== payload.reviewType) reasons.push(`reviewType ${payload.reviewType} 与门禁 ${t.gate} 不符`);

  // G6 assignee concrete user (must start with @, not a bare role)
  const ROLES = ['产品', '研发', '测试'];
  if (!payload.assigneeUser || !payload.assigneeUser.startsWith('@') || ROLES.includes(payload.assigneeUser)) {
    reasons.push('Assignee 必须是具体 GitLab 用户(@xxx)，不能是角色名');
  }

  // G10 date confirmation
  const hasDate = Object.keys(payload.fields).some((k) => /日期|Date/.test(k) || k.includes('日期'));
  if (hasDate && !payload.datesConfirmed) reasons.push('日期需用户确认后才能落盘(datesConfirmed)');

  // G11 blocking test issues verified (field must be 是)
  if (payload.from === '测试中' && payload.to === '待发布') {
    if (payload.fields['阻塞发布问题均已验证通过'] !== '是') reasons.push('存在未验证的阻塞发布问题，不得进入 待发布');
  }

  // G12 terminal atomicity
  if (t.terminal && !payload.closeIssue) reasons.push('终态需同一次操作关闭 Issue(closeIssue)');

  if (missing.length || reasons.length) return fail(reasons, missing);
  return ok;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm vitest run engine/src/guard.test.ts`
Expected: PASS (all).

- [ ] **Step 5: Commit**

```bash
git add engine/src/guard.ts engine/src/guard.test.ts
git commit -m "feat(guard): G5 labels, G6 assignee, G9 no-placeholder, G10 date, G11 blocking, G12 terminal"
```

### Task 2.3: Write-plan guards G7, G8, G13 (TDD)

**Files:**
- Modify: `engine/src/guard.ts`, `engine/src/guard.test.ts`

- [ ] **Step 1: Append failing tests**

```ts
import { validateWritePlan } from './guard.js';
import type { WritePlan } from './types.js';

describe('G7/G8/G13 write-plan guards', () => {
  const plan = (ops: WritePlan['ops']): WritePlan => ({ issueIid: 123, ops });

  it('G7 forbids editing original issue body', () => {
    const r = validateWritePlan(plan([{ kind: 'add_label', value: 'x' }, { kind: 'add_comment', body: 'hi' }]));
    expect(r.ok).toBe(true);
    // an edit-body op would be rejected, but WriteOp union has no edit-body kind → type-safe by construction
  });
  it('G8 forbids editing existing comments (no edit_comment op allowed)', () => {
    // edit_comment is not in the WriteOp union; validateWritePlan rejects unknown kinds
    const r = validateWritePlan(plan([{ kind: 'edit_comment', body: 'x' } as any]));
    expect(r.ok).toBe(false);
  });
  it('G13 forbids creating Jira', () => {
    const r = validateWritePlan(plan([{ kind: 'create_jira' } as any]));
    expect(r.ok).toBe(false);
  });
  it('accepts a clean label+assignee+comment+close plan', () => {
    const r = validateWritePlan(plan([
      { kind: 'remove_label', value: 'story-status::生产验收中' },
      { kind: 'add_label', value: 'story-status::已完成' },
      { kind: 'set_assignee', username: '@pm' },
      { kind: 'add_comment', body: '## 状态变更' },
      { kind: 'close_issue' },
    ]));
    expect(r.ok).toBe(true);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run engine/src/guard.test.ts`
Expected: FAIL (validateWritePlan undefined).

- [ ] **Step 3: Add `validateWritePlan` to `engine/src/guard.ts`**

```ts
import type { WritePlan, WriteOp } from './types.js';

const ALLOWED_OPS = new Set<WriteOp['kind']>(['add_label', 'remove_label', 'set_assignee', 'add_comment', 'close_issue']);
const FORBIDDEN = new Set(['edit_comment', 'edit_issue_body', 'update_issue_body', 'create_jira', 'delete_comment']);

export function validateWritePlan(plan: WritePlan): GuardResult {
  const reasons: string[] = [];
  for (const op of plan.ops) {
    const k = (op as { kind: string }).kind;
    if (FORBIDDEN.has(k)) reasons.push(`禁止的操作 ${k}（G7/G8/G13：不改原文/不编评论/不建Jira）`);
    else if (!ALLOWED_OPS.has(k as WriteOp['kind'])) reasons.push(`未知操作 ${k}`);
  }
  return reasons.length ? fail(reasons) : ok;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm vitest run engine/src/guard.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add engine/src/guard.ts engine/src/guard.test.ts
git commit -m "feat(guard): G7/G8/G13 write-plan guards (no body/comment edit, no Jira)"
```

---

## Phase 3 — GitLab integration layer (mock)

### Task 3.1: GitLab client — read issue + parse 交付协同 (TDD)

**Files:**
- Create: `engine/src/gitlab.ts`, `engine/src/gitlab.test.ts`, `engine/fixtures/issue-story-pending-review.json`

- [ ] **Step 1: Write fixture `engine/fixtures/issue-story-pending-review.json`**

```json
{
  "iid": 123,
  "state": "opened",
  "labels": ["type::story", "story-status::待评审", "triage::done"],
  "description": "# 需求\n...\n## 交付协同\n\n| 角色 | GitLab 用户 |\n| --- | --- |\n| 产品 | @vicky |\n| 研发 | @eliojin |\n| 测试 | @qa |\n"
}
```

- [ ] **Step 2: Write failing test `engine/src/gitlab.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { parseAssigneeTable, type GitLabIssue } from './gitlab.js';

const issue = JSON.parse(readFileSync(new URL('./fixtures/issue-story-pending-review.json', import.meta.url), 'utf8')) as GitLabIssue;

describe('gitlab read/parse', () => {
  it('parses 交付协同 role->user table from description', () => {
    const map = parseAssigneeTable(issue.description);
    expect(map.get('产品')).toBe('@vicky');
    expect(map.get('研发')).toBe('@eliojin');
    expect(map.get('测试')).toBe('@qa');
  });
  it('returns empty map when table absent', () => {
    expect(parseAssigneeTable('no table here').size).toBe(0);
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `pnpm vitest run engine/src/gitlab.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 4: Write `engine/src/gitlab.ts` (parse first; HTTP in Task 3.2)**

```ts
import type { IssueFacts, WriteOp, WritePlan } from './types.js';

export interface GitLabIssue {
  iid: number;
  state: 'opened' | 'closed';
  labels: string[];
  description: string;
}

export function toFacts(issue: GitLabIssue): IssueFacts {
  return {
    labels: issue.labels,
    body: issue.description,
    state: issue.state,
    hasJiraSourceLabel: issue.labels.includes('source::jira'),
  };
}

export function parseAssigneeTable(description: string): Map<string, string> {
  const map = new Map<string, string>();
  const lines = description.split('\n');
  let inTable = false;
  for (const line of lines) {
    const cells = line.split('|').map((c) => c.trim()).filter(Boolean);
    if (line.includes('交付协同')) { inTable = true; continue; }
    if (inTable) {
      if (cells.length === 0) continue;
      if (cells[0]?.startsWith('---')) continue; // separator
      if (cells[0] === '角色') continue;
      if (cells.length >= 2 && cells[0] && /^@\w+/.test(cells[1] ?? '')) map.set(cells[0], cells[1]!);
      if (!line.startsWith('|')) break; // table ended
    }
  }
  return map;
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `pnpm vitest run engine/src/gitlab.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add engine/src/gitlab.ts engine/src/gitlab.test.ts engine/fixtures/issue-story-pending-review.json
git commit -m "feat(gitlab): parse 交付协同 role->user table + toFacts"
```

### Task 3.2: GitLab boundary — keep engine pure, leave writeback to Leader glab CLI

**Current architecture note:** this historical task was superseded by the v1.3 boundary: `engine/src/gitlab.ts` is parse/facts-only, and the CLI has no writeback command. The engine builds and validates `WritePlan` JSON; Leader previews it, gets confirmation, and performs GitLab read/write with the authenticated `glab` CLI. Keep tests focused on this boundary instead of adding a network client to the engine.

**Files:**
- Verify: `engine/src/gitlab.ts`, `engine/src/gitlab.test.ts`, `engine/src/process-contract.test.ts`

- [ ] Assert production engine files do not import subprocess/network modules or call `glab`.
- [ ] Assert CLI command docs list only pure commands: `node`, `validate`, `render`, `plan`, `plan-return`, `evidence`, `config`, `state-init`.
- [ ] Assert WritePlan operations remain previewable data; the Leader translates them to `glab issue update` / `glab issue note` / `glab issue close` outside the engine.

### Task 3.3: Render structured payload → harness markdown (TDD)

**Files:**
- Create: `engine/src/render.ts`, `engine/src/render.test.ts`

- [ ] **Step 1: Write failing test**

```ts
import { describe, it, expect } from 'vitest';
import { renderStatusChange, renderReturn } from './render.js';
import type { Payload } from './types.js';

describe('render', () => {
  it('renders a 状态变更 comment from structured fields', () => {
    const p: Payload = { type: 'story', from: '待评审', to: '已评审',
      fields: { 评审日期: '2026-07-28', 产品确认人: '@pm', 评审结论: '通过', 需求文档或评审记录: 'link' },
      assigneeUser: '@dev' };
    const md = renderStatusChange(p);
    expect(md).toContain('## 状态变更');
    expect(md).toContain('`待评审` → `已评审`');
    expect(md).toContain('2026-07-28');
    expect(md).toContain('@pm');
  });
  it('renders a 退回 comment with the 问题清单', () => {
    const md = renderReturn('草稿中', ['范围未明确', '验收标准缺失'], '@pm', '2026-07-28');
    expect(md).toContain('退回：`草稿中`');
    expect(md).toContain('范围未明确');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run engine/src/render.test.ts`
Expected: FAIL.

- [ ] **Step 3: Write `engine/src/render.ts`**

```ts
import type { Payload } from './types.js';

export function renderStatusChange(p: Payload): string {
  const f = p.fields;
  const lines = ['## 状态变更', '', `- 变更：\`${p.from}\` → \`${p.to}\``];
  const dateKey = Object.keys(f).find((k) => k.includes('日期')) ?? '';
  if (dateKey) lines.push(`- 实际日期：${f[dateKey]}`);
  const who = f['产品确认人'] ?? f['测试Assignee'] ?? f['研发Assignee'] ?? p.assigneeUser ?? '';
  if (who) lines.push(`- 确认人：${who}`);
  const concl = f['评审结论'] ?? f['测试结论'] ?? f['验收结论'] ?? f['验证结论'];
  if (concl) lines.push(`- 结论：${concl}`);
  const ev = f['需求文档或评审记录'] ?? f['回归范围或证据'] ?? f['验收依据'] ?? f['验证依据'] ?? f['发布记录或回滚信息'];
  if (ev) lines.push(`- 依据：${ev}`);
  if (p.assigneeUser) lines.push(`- 目标节点 Assignee：${p.assigneeUser}`);
  return lines.join('\n');
}

export function renderReturn(target: string, issues: string[], confirmer: string, date: string): string {
  return ['## 状态变更（退回）', '',
    `- 退回：\`${target}\``, `- 实际日期：${date}`, `- 确认人：${confirmer}`,
    '', '### 问题清单（需修订）', ...issues.map((i, n) => `${n + 1}. ${i}`),
    '', '- 下一步：产品修订后重新进入待评审'].join('\n');
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run engine/src/render.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add engine/src/render.ts engine/src/render.test.ts
git commit -m "feat(render): structured payload -> harness 状态变更/退回 markdown"
```

---

## Phase 4 — CLI + skill/agent pack

### Task 4.1: CLI entrypoint

**Files:**
- Create: `engine/src/cli.ts`

- [ ] **Step 1: Write `engine/src/cli.ts`**

```ts
import { loadModel } from './model.js';
import { validateTransition, validateWritePlan } from './guard.js';
import { toFacts } from './gitlab.js';
import { renderStatusChange } from './render.js';

const model = loadModel();

function readStdin(): string { return require('node:fs').readFileSync(0, 'utf8'); }  // eslint-disable-line

async function main() {
  const [, , cmd, ...args] = process.argv;
  switch (cmd) {
    case 'node': {
      // glab-flow node <type> <labels...>
      const [type, ...labels] = args;
      const { currentNode } = await import('./model.js');
      const node = currentNode(model, type as 'story' | 'bug', labels);
      console.log(JSON.stringify({ node }));
      break;
    }
    case 'validate': {
      const input = JSON.parse(readStdin()) as { type: 'story' | 'bug'; labels: string[]; payload: import('./types.js').Payload };
      const result = validateTransition(model, toFacts({ iid: 0, state: 'opened', labels: input.labels, description: '' }), input.payload);
      console.log(JSON.stringify(result));
      break;
    }
    case 'render': {
      const payload = JSON.parse(readStdin()) as import('./types.js').Payload;
      console.log(renderStatusChange(payload));
      break;
    }
    case 'plan': {
      // stdin: { payload } -> prints validated WritePlan JSON; Leader applies it with glab after preview.
      const input = JSON.parse(readStdin()) as { payload: import('./types.js').Payload };
      const p = input.payload;
      const plan = {
        issueIid: Number(args[0] ?? 0),
        ops: [
          { kind: 'remove_label', value: `${p.type === 'story' ? 'story-status' : 'status'}::${p.from}` },
          { kind: 'add_label', value: `${p.type === 'story' ? 'story-status' : 'status'}::${p.to}` },
          { kind: 'set_assignee', username: p.assigneeUser ?? '' },
          { kind: 'add_comment', body: renderStatusChange(p) },
          ...(p.closeIssue ? [{ kind: 'close_issue' as const }] : []),
        ],
      };
      const guard = validateWritePlan(plan as import('./types.js').WritePlan);
      if (!guard.ok) { console.error(JSON.stringify(guard)); process.exit(1); }
      console.log(JSON.stringify(plan));
      break;
    }
    default:
      console.error('commands: node | validate | render | plan | plan-return | evidence | config | state-init');
      process.exit(1);
  }
}
main().catch((e) => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: Smoke test the CLI (offline commands)**

Run:
```bash
cd /Users/eliojin/IdeaProjects/glab-flow
echo '{"type":"story","labels":["type::story","story-status::待评审"],"payload":{"type":"story","from":"待评审","to":"已评审","fields":{"评审日期":"2026-07-28","产品确认人":"@pm","评审结论":"通过","需求文档或评审记录":"link"},"gateOutcome":"通过","reviewType":"需求评审","assigneeUser":"@dev","datesConfirmed":true}}' | pnpm cli validate
```
Expected: `{"ok":true,"missing":[],"reasons":[]}`

- [ ] **Step 3: Commit**

```bash
git add engine/src/cli.ts
git commit -m "feat(cli): add pure node/validate/render/plan commands"
```

### Task 4.2: glab-flow skill (Leader orchestration)

**Files:**
- Create: `skills/glab-flow/SKILL.md`, `skills/glab-flow/nodes.md`, `skills/glab-flow/guards.md`

- [ ] **Step 1: Write `skills/glab-flow/SKILL.md`**

```markdown
---
name: glab-flow
description: 当用户提供 oa-ai-native-harness 的 GitLab Issue URL/编号，需要按 harness 状态机驱动需求从分诊到上线/验收时使用。本地人驱动，每次写回 GitLab 前预览确认，护栏由 engine 确定性校验。编排多 agent，Leader 只委托。
---

# glab-flow：harness GitLab 状态机驱动引擎

**输入**：`$ARGUMENTS` = GitLab Issue URL 或 iid（如 `https://git.example.com/group/project/-/issues/<iid>` 或 `<iid>`）。

**引擎**：`/Users/eliojin/IdeaProjects/glab-flow`，命令前缀 `cd /Users/eliojin/IdeaProjects/glab-flow && pnpm cli <cmd>`。引擎命令均为纯计算；GitLab 读写由 Leader 使用已认证的 `glab` CLI 完成，无 token 环境变量要求。

## Leader 每轮编排（严格按序）

1. **读状态**：Leader 用 `glab issue view <iid> --output json` 或 `glab api --hostname <host> ...` 读取 Issue 的 labels/description/comments；再用 engine `node` 命令得当前节点。
   - 当前节点 = labels 里 `story-status::*`（需求）或 `status::*`（Bug）去掉前缀。0 个或 ≥2 个 → **脏状态**，停止，列给人工，不推进。
2. **查契约**：从 `nodes.md` 取「当前节点 → 允许的下一节点 / 必填项 / 门禁 / Assignee 角色」。
3. **判断能否流转**：
   - 证据齐全 → 委派**流转 agent** 起草结构化 Payload（字段从 Issue 评论/证据抽取，人/结论不得臆造）。
   - 不齐 → 委派**节点工作 agent** 生成本节点内容（见 `nodes.md`：草稿/方案/代码/测试），写回用评论/分支，不推进状态。
4. **护栏校验**：把 Payload 喂 `pnpm cli validate`。`ok:false` → **停**，一次性列出 `missing`+`reasons` 问用户，不写。
5. **构建写计划**：`pnpm cli plan <iid>`（stdin: labels+payload）→ 得 WritePlan JSON。
6. **预览确认**：把 WritePlan 渲染成 diff 给用户看（add/remove label、set assignee、add comment、close）。用户 `y` 才继续；`n`/编辑 → 回到 Payload 收集。
7. **应用**：Leader 将 WritePlan 预览确认后翻译为 `glab issue update` / `glab issue note` / `glab issue close`；引擎没有 `apply` 命令。
8. **下一节点**：循环到 `已完成` 或用户停。

## 硬规则（不可违反，详见 `guards.md`）

- **三类评审分离**：技术方案评审通过**只记录**，不得据此进「开发中」；代码评审不推进状态。
- **门禁二值**：需求评审结论为「退回」→ 写退回评论（含问题清单）回「草稿中」，绝不进下一节点。
- **hard_gate（待发布/验收/关闭）**：无人工证据 + humanConfirmed 不得写。
- **冻结**：永不编辑已存在评论/原文，只新增评论或退回建新版本。
- **日期**：自动带出本地日期为候选，用户「确认」后才落盘。
- **Assignee**：从 Issue「交付协同」表解析具体 @用户，缺则反问，不接受角色名。
- **不建 Jira**；测试问题挂父需求评论，不建 Bug Issue。

## 复用的现有 skill/agent

需求/方案 → `spec-author`；开发 → `git-ops`+`tdd-guide`+codegraph；代码评审 → `code-review`/`php-reviewer`/`java-reviewer`/`typescript-reviewer`；测试 → `test-design`/`test-flow-apifox`；发布 → `jenkins-deploy`。本 skill 自带 agent：`intake`、`review-preview`、`release-check`。
```

- [ ] **Step 2: Write `skills/glab-flow/nodes.md`** (condense spec §6.2 table)

```markdown
# 节点契约（引擎权威来源 = engine/state-machine.yaml；本表是 Leader 速查）

| 节点 | 工作agent | 产出 | 门禁 | 流转写回 |
|---|---|---|---|---|
| 分诊 triage::pending | intake | 澄清问题+建议分类 | 团队确认 | type::story+草稿中, Assignee=产品 |
| 草稿中 | spec-author | 需求草稿(六清楚) | 草稿门槛 | →待评审 |
| 待评审 | review-preview | 评审意见+问题清单 | 需求评审(二值) | 通过→已评审 / 退回→草稿中 |
| 已评审 | spec-author/architect | 技术方案design.md | 技术方案评审(只记录)+开发门槛 | 进开发 Assignee=研发 |
| 开发中 | git-ops+tdd-guide+codegraph | 代码+MR描述+自测 | 代码评审与自测 | →测试中, Assignee=测试 |
| 测试中 | test-design/test-flow-apifox | 测试计划;测试问题评论 | 测试验收(阻塞全验证) | →待发布, Assignee=研发 |
| 待发布 | release-check+jenkins-deploy | 风险/检查清单/回滚 | 发布(hard_gate) | →生产验收中, Assignee=产品 |
| 生产验收中→已完成 | Leader起草终态评论 | 验收记录 | 产品验收(hard_gate·terminal) | →已完成+关闭, Assignee=产品; 反哺context/faq/cases |
```

- [ ] **Step 3: Write `skills/glab-flow/guards.md`** (G1–G13 plain-language, mirrors engine)

```markdown
# 护栏 G1–G13（确定性，由 engine/src/guard.ts 强制；本表给 Leader 参考）

G1 流转前置必填 | G2 门禁二值(通过/退回) | G3 hard_gate需humanConfirmed | G4 三类评审分离
G5 标签唯一(脏状态拒绝) | G6 Assignee具体@用户 | G7 不改原文 | G8 不编评论
G9 不臆造人/结论(禁"待确认") | G10 日期需确认 | G11 阻塞问题全验证才放行 | G12 终态原子(标签+Assignee+评论+关闭同次) | G13 不建Jira
```

- [ ] **Step 4: Commit**

```bash
git add skills/glab-flow
git commit -m "feat(skill): glab-flow SKILL.md + nodes/guards reference"
```

### Task 4.3: glab-flow expert agents (thin, harness-spec-based)

**Files:**
- Create: `agents/intake.md`, `agents/review-preview.md`, `agents/release-check.md`

- [ ] **Step 1: Write `agents/intake.md`**

```markdown
---
name: intake
description: glab-flow 分诊/澄清 agent。读 triage Issue，产出澄清问题 + 建议分类(需求/Bug/重复) + 建议 Assignee。不自行定性。
tools: Read, Bash, Grep, Glob
model: sonnet
---

你是 glab-flow 的分诊 agent。输入：一个 `triage::pending` 的 GitLab Issue（标题+正文+附件）。

任务：
1. 基于 oa-ai-native-harness `oa-knowledge-intake` 规格，整理「事项描述/场景影响/已有证据」。
2. 列出**澄清问题**（目标系统、菜单路径、角色权限、是否生产缺陷等），缺啥问啥。
3. 给**建议分类**：需求 / 线上Bug / 重复·无需处理（仅建议，附理由；最终由团队确认）。
4. 建议 Assignee 角色（产品/研发/测试）。

约束：不得自行加 `type::story`/`type::bug`；不得覆盖原始 Issue；只产出建议评论草稿，交 Leader 预览确认。
```

- [ ] **Step 2: Write `agents/review-preview.md`**

```markdown
---
name: review-preview
description: glab-flow 需求评审预审 agent。对照 harness 评审门槛预审需求草稿，产出评审意见 + 问题清单。不决定通过/退回。
tools: Read, Bash, Grep, Glob
model: sonnet
---

你是 glab-flow 的需求评审预审 agent。输入：需求草稿（Issue 正文）。

任务：对照 harness 门槛逐条核对——背景清楚 / 范围清楚 / 不做范围清楚 / 验收标准清楚 / 适用口径清楚 / Owner清楚，以及准入原则（有业务场景/有Owner/有适用范围/有生效日期/有复查机制/有反馈入口）。

产出：
- **评审意见**：每条「位置/缺口/建议」。
- **问题清单**：低质量草稿问题多 → 建议**退回**（仍由产品拍板）。

约束：只预审、不决策通过/退回；不编辑原文。
```

- [ ] **Step 3: Write `agents/release-check.md`**

```markdown
---
name: release-check
description: glab-flow 发布检查 agent。发布前检查发布风险（审批流/权限/财务/合同/通知），产出风险等级+必补事项+发布后检查清单+回滚方案。基于 harness oa-release-check 规格。
tools: Read, Bash, Grep, Glob
model: sonnet
---

你是 glab-flow 的发布检查 agent。输入：待发布 Issue + 变更范围 + MR。

产出（对照 oa-release-check）：发布风险等级、必须补充事项、发布后检查清单、回滚方式。高风险变更标注需人工确认。
```

- [ ] **Step 4: Commit**

```bash
git add agents
git commit -m "feat(agents): intake, review-preview, release-check"
```

### Task 4.4: install / uninstall scripts

**Files:**
- Create: `install.sh`, `uninstall.sh`

- [ ] **Step 1: Write `install.sh`**

```bash
#!/bin/bash
# glab-flow installer — symlinks skill + agents into ~/.claude
set -e
DIR="$(cd "$(dirname "$0")" && pwd)"
CLAUDE="$HOME/.claude"
mkdir -p "$CLAUDE/skills" "$CLAUDE/agents"
ln -sf "$DIR/skills/glab-flow" "$CLAUDE/skills/glab-flow"
for a in intake review-preview release-check; do
  ln -sf "$DIR/agents/$a.md" "$CLAUDE/agents/$a.md"
done
echo "[glab-flow] installed (skill + 3 agents). Engine: cd $DIR && pnpm install"
echo "[glab-flow] GitLab auth is handled by glab CLI; run glab auth status if needed"
```

- [ ] **Step 2: Write `uninstall.sh`**

```bash
#!/bin/bash
set -e
CLAUDE="$HOME/.claude"
rm -f "$CLAUDE/skills/glab-flow" "$CLAUDE/agents/intake.md" "$CLAUDE/agents/review-preview.md" "$CLAUDE/agents/release-check.md"
echo "[glab-flow] uninstalled"
```

- [ ] **Step 3: Make executable and commit**

```bash
chmod +x install.sh uninstall.sh
git add install.sh uninstall.sh
git commit -m "chore: install/uninstall scripts (symlink into ~/.claude)"
```

---

## Phase 5 — End-to-end fixture

### Task 5.1: E2E — 分诊 → 草稿 → 待评审(退回) → 已评审

**Files:**
- Create: `engine/src/e2e.test.ts`, `engine/fixtures/issue-story-draft.json`

- [ ] **Step 1: Write fixture `engine/fixtures/issue-story-draft.json`**

```json
{ "iid": 200, "state": "opened", "labels": ["type::story", "story-status::草稿中", "triage::done"],
  "description": "# 需求\n## 交付协同\n\n| 角色 | GitLab 用户 |\n| --- | --- |\n| 产品 | @pm |\n| 研发 | @dev |\n| 测试 | @qa |\n" }
```

- [ ] **Step 2: Write failing e2e test `engine/src/e2e.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { loadModel } from './model.js';
import { currentNode } from './model.js';
import { validateTransition, validateWritePlan } from './guard.js';
import { toFacts, parseAssigneeTable, type GitLabIssue } from './gitlab.js';
import { renderStatusChange, renderReturn } from './render.js';

const model = loadModel();
const issue = JSON.parse(readFileSync(new URL('./fixtures/issue-story-draft.json', import.meta.url), 'utf8')) as GitLabIssue;

describe('e2e: 草稿中 -> 待评审 -> (退回) 草稿中 -> 待评审 -> 已评审', () => {
  it('drives the loop with guards + render + plan validation', () => {
    // step 1: current node
    expect(currentNode(model, 'story', issue.labels)).toBe('草稿中');

    // step 2: forward 草稿中->待评审 needs nothing required → ok
    let facts = toFacts(issue);
    let r = validateTransition(model, facts, { type: 'story', from: '草稿中', to: '待评审', fields: {}, assigneeUser: '@pm' });
    expect(r.ok).toBe(true);

    // step 3: at 待评审, review returns 退回 → must NOT advance to 已评审
    facts = { ...facts, labels: ['type::story', 'story-status::待评审'] };
    r = validateTransition(model, facts, { type: 'story', from: '待评审', to: '已评审', fields: {}, gateOutcome: '退回' });
    expect(r.ok).toBe(false);

    // step 4: render 退回 comment with 问题清单
    const ret = renderReturn('草稿中', ['验收标准缺失'], '@pm', '2026-07-28');
    expect(ret).toContain('验收标准缺失');

    // step 5: after fix, 需求评审 通过 → 已评审 ok
    r = validateTransition(model, facts, {
      type: 'story', from: '待评审', to: '已评审',
      fields: { 评审日期: '2026-07-28', 产品确认人: '@pm', 评审结论: '通过', 需求文档或评审记录: 'doc' },
      gateOutcome: '通过', reviewType: '需求评审', assigneeUser: '@dev', datesConfirmed: true,
    });
    expect(r.ok).toBe(true);

    // step 6: build + validate a write plan for 已评审 transition
    const plan = { issueIid: 200, ops: [
      { kind: 'remove_label', value: 'story-status::待评审' },
      { kind: 'add_label', value: 'story-status::已评审' },
      { kind: 'set_assignee', username: '@dev' },
      { kind: 'add_comment', body: renderStatusChange({ type: 'story', from: '待评审', to: '已评审',
        fields: { 评审日期: '2026-07-28', 产品确认人: '@pm', 评审结论: '通过', 需求文档或评审记录: 'doc' }, assigneeUser: '@dev' }) },
    ] };
    expect(validateWritePlan(plan).ok).toBe(true);

    // step 7: assignee resolved from 交付协同
    expect(parseAssigneeTable(issue.description).get('研发')).toBe('@dev');
  });
});
```

- [ ] **Step 3: Run the full suite**

Run: `pnpm test`
Expected: ALL PASS (model, contract, guard, gitlab, render, e2e).

- [ ] **Step 4: Typecheck**

Run: `pnpm typecheck`
Expected: no errors. Fix the `require('node:fs')` in cli.ts if it errors under ESM (replace with `import { readFileSync } from 'node:fs'` at top).

- [ ] **Step 5: Commit**

```bash
git add engine/src/e2e.test.ts engine/fixtures/issue-story-draft.json engine/src/cli.ts
git commit -m "test(e2e): 分诊->草稿->待评审(退回)->已评审 with guards+render+plan"
```

---

## Phase 6 — Docs + ship-ready

### Task 6.1: README, architecture doc, sample run

**Files:**
- Modify: `README.md`, Create: `docs/architecture.md`

- [ ] **Step 1: Expand `README.md`**

Add sections: Architecture (pointer to spec + docs/architecture.md), pure engine commands (`pnpm cli node|validate|render|plan|plan-return|evidence|config|state-init`), glab CLI authentication (`glab auth status`, no token env), Install (`./install.sh`), Test (`pnpm test`), Relation to dev-flow (coexist; GitLab-native).

- [ ] **Step 2: Write `docs/architecture.md`**

Copy spec §4 (seven layers + dataflow) and §7.2 (G1–G13 table) verbatim as the operator reference.

- [ ] **Step 3: Final full-suite run**

Run:
```bash
cd /Users/eliojin/IdeaProjects/glab-flow
pnpm install
pnpm typecheck && pnpm test
```
Expected: typecheck clean; all tests pass.

- [ ] **Step 4: Commit**

```bash
git add README.md docs/architecture.md
git commit -m "docs: README + architecture reference"
```

---

## Self-Review (completed by plan author)

**1. Spec coverage:**
- §3 decisions → Task 0.1 (TS/hybrid), 4.2 (skill), 2.x (guard)
- §4 architecture (7 layers) → File Structure + Task 4.2 SKILL.md + engine modules
- §5 state machine → Task 1.1 (yaml), 1.2 (model), 1.3 (contract)
- §6 lifecycle (per-node + algorithm) → Task 4.2 (SKILL.md algorithm) + nodes.md
- §7 guardrails G1–G13 → Tasks 2.1/2.2/2.3
- §8 v1 scope/acceptance → Phase 5 e2e covers acceptance bullet 1 & 4; bullets 2/3/5/6 covered by guard tests + SKILL.md
- §9 persistence → SKILL.md (GitLab truth) + .gitignore (.glab-flow/)
- §10 testing → Phases 1–3 + 5

**Gap found & addressed:** acceptance bullet 2 (已评审→开发 cross-repo) — covered by reusing git-ops/codegraph in SKILL.md/nodes.md, not a new engine task (it's orchestration of existing skills). Acceptable.

**2. Placeholder scan:** none — every code step has real code, every command has expected output.

**3. Type consistency:** `Payload`, `WriteOp`, `WritePlan`, `IssueFacts`, `GuardResult` defined in Task 1.2 types.ts and used consistently in guard/gitlab/render/cli. `assigneeRole` / `currentNode` / `transitionFor` signatures match across model.ts and tests. `validateTransition` / `validateWritePlan` signatures match tests.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-28-glab-flow.md`.
